
## 2. Check if Array Contains Duplicate
**Problem:**  
Return `true` if the array has duplicate elements, otherwise return `false`.  

**Instructions:**  
- Use a **HashSet** to track seen numbers.  
- If a number is already in the set, return `true`.  

**Examples:**  
- Input: `[1, 2, 3, 1]` → Output: `true`  
- Input: `[1, 2, 3, 4]` → Output: `false`  
- Input: `[10, 20, 30, 40, 10]` → Output: `true`  
